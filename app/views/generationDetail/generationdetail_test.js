'use strict';

describe('ngApp.generationDetail module', function() {

  beforeEach(module('ngApp.generationDetail'));

  describe('generationDetail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var generationDetailCtrl = $controller('GenerationDetailCtrl');
      expect(generationDetailCtrl).toBeDefined();
    }));

  });
});