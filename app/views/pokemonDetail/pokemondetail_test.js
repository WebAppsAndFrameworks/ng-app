'use strict';

describe('ngApp.pokemonDetail module', function() {

  beforeEach(module('ngApp.pokemonDetail'));

  describe('pokemonDetail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pokemonDetailCtrl = $controller('PokemonDetailCtrl');
      expect(pokemonDetailCtrl).toBeDefined();
    }));
  });
});
